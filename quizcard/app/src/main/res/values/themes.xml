<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Quizcard" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:textColor">#181818</item>
    </style>
    <style name="Theme.other" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:textColor">#FFFFFF</item>
    </style>
    <style name="Theme.QuizCard.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/green</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.Quizcard</item>
    </style>


</resources>